version: '3.3'

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - '5000:3000'

  auth:
    build:
      context: ./auth
      dockerfile: Dockerfile
    ports:
      - '5001:3000'

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    ports:
      - '5002:3000'
  
  forms:
    build:
      context: ./forms
      dockerfile: Dockerfile
    ports:
      - '5003:3000'
